#!/bin/bash
set -e

exeName=postTX
repoName=${exeName,,}
imgName=teamfae/$repoName

mkdir -p bin/
stack --local-bin-path $PWD/bin --flag fae:static install :$exeName
{ docker build -t $imgName . ; s=$?; } || true
rm -r ./bin
exit $s
